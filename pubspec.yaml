name: brand_logos
description: Brand Logos provides more then 700 brands logos in one place. It provides you easy way to add brand logo in your dream project.
version: 0.0.1
homepage: https://github.com/3kdeveloper/brand_logos

environment:
  sdk: '>=2.18.0 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^1.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:

  assets:
    - assets/brands/